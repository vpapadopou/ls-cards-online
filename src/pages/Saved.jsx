import React from 'react';

import { ScrollArea } from '@/components/ui/scroll-area';

function Saved() {
  return <ScrollArea className="h-[calc(100vh-60px)]">Saved</ScrollArea>;
}

export default Saved;
