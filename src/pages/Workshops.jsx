import React from 'react';

import { ScrollArea } from '@/components/ui/scroll-area';

function About() {
  return <ScrollArea className="h-[calc(100vh-60px)]">Workshops</ScrollArea>;
}

export default About;
